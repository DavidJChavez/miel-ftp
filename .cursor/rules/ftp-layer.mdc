---
description: Capa FTP — suppaftp, logs y respuestas
globs: "src/ftp/**/*.rs"
alwaysApply: false
---

# FTP — `src/ftp/`

## Respuesta estándar

```rust
pub struct FtpResponse<T> {
    pub result: AppResult<T>,
    pub log: Vec<String>,
}
```

Registrar comandos semánticos: `> CONNECT`, `> USER`, `PASS ***`, `> CWD`, `> LIST`, `> STOR`, `> RETR`, `> QUIT`. En error: `< OPERATION ERROR: ...`.

El reducer aplica `log` con `apply_ftp_log` — no duplicar logging solo en `tracing`.

## Listados

Usar `FtpEntry::from_list_line` (`suppaftp::list::File`). No parsear LIST a mano.

## Login

```rust
ftp.login(conn.username.as_str(), conn.password()).await?;
```

## Sesión persistente

- Usar `FtpSessionManager` en `State` (`state.ftp_manager`).
- Un `AsyncFtpStream` por `connection.id`; log `> sesión reutilizada` en operaciones subsecuentes.
- `disconnect` / `disconnect_all_except` al cambiar de sitio o desconectar.

## Pendiente (no implementar a medias)

- Streaming upload/download (no `read_to_end` / `read` entero del archivo).
- FTPS: `into_secure()` cuando se pida explícitamente.

## Timeouts y reintentos

- Constantes en `ftp/config.rs` (`CONNECT_TIMEOUT`, `OPERATION_TIMEOUT`, `MAX_RETRIES`).
- `with_timeout` y macro `ftp_retry!` en `manager.rs`; invalidar sesión antes de reintentar.
